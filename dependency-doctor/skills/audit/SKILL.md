---
name: audit
description: List all dependencies in the current project with versions.
disable-model-invocation: true
allowed-tools: mcp__dependency-doctor__audit_project
argument-hint: (no arguments needed)
---

Call `mcp__dependency-doctor__audit_project` with `cwd` = current working directory.

Display the result clearly. Suggest using `/dependency-doctor:analyze <package>` to check individual packages.
