---
name: stats
description: Show pair programming stats — edits, approvals, rejections, approval rate, session duration.
disable-model-invocation: true
allowed-tools: mcp__pair-mode__get_stats
argument-hint: (no arguments needed)
---

Call `mcp__pair-mode__get_stats` with `cwd` = current working directory.

Format and display the result.
